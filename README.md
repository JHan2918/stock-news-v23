# Stock News Event Core v24 Render

Render 배포용 버전입니다.

## Render 설정

- Build Command: `pip install -r requirements.txt`
- Start Command: `python app.py`

Render는 자동으로 `PORT` 환경변수를 제공합니다.
앱은 `0.0.0.0:$PORT`로 실행됩니다.
