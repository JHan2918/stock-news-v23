services:
  - type: web
    name: stock-news-mobile-radar
    env: python
    plan: free
    rootDir: mobile-radar
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: DATA_DIR
        value: ../data
